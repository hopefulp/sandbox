###
raw_input() was renamed to input() in common.py

f_modify.py a.py python -o option
    modify shebang of a.py
    confer $HOST for chi(/home/joonho), login(mlet::/grfs/home/joonho)
    if '#!' use "sed -i '1s[.*[...' fname" for substitution (not ready for python)
    else    add new 1st line
    def add_first_line
